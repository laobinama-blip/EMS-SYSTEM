# EMS KPI Frontend

React + Vite + TypeScript frontend for the EMS KPI dashboard.

## Scripts

```bash
npm run dev
npm run typecheck
npm run build
```

The first implementation is fixture-driven so the four prototype pages can be reviewed before backend integration.
